# Where the resource limits live.
#
# They are here rather than in a Makefile on purpose: a `docker run --memory=8g`
# buried in a shell string is a limit someone forgets to copy, and the failure
# mode of forgetting is the one that took the machine down. In a compose file
# they are configuration, under version control, and they apply to every
# invocation without anyone choosing to apply them.
#
# TRAP, and it is a quiet one: `deploy.resources.limits.memory` is IGNORED by
# `docker compose` outside Swarm. Written that way this file would run with no
# memory limit at all and say nothing. The service-level `mem_limit` and
# `memswap_limit` keys below are the ones actually honoured.

services:
  sonic:
    build:
      context: .
    image: sonic-scheme:dev
    init: true                 # tini as PID 1: signals forwarded, zombies reaped

    # 8g is far above what a real compile needs -- the suite runs in well under
    # a gigabyte -- and far below what it takes to hurt the host, so it only
    # ever fires on a runaway.
    mem_limit: 8g
    # MUST equal mem_limit. Left unset, Docker defaults swap to 2x memory, and
    # the container thrashes its way to the same death instead of dying at the
    # limit. This one line is the difference between a fast failure and a slow
    # one.
    memswap_limit: 8g

    cpus: 8
    pids_limit: 512            # a runaway that forks rather than conses

    # The suite reaches nothing over the network, so it is given none. Two
    # things fall out. Rootless podman otherwise leaves a netns behind that it
    # cannot always tear down -- `rootless netns: kill network process:
    # permission denied` on every run, a `level=error` line that is pure noise
    # and would train a reader to ignore errors in CI logs. And a compile that
    # suddenly wants the network is a compile doing something it should be
    # made to justify.
    network_mode: none

    volumes:
      - .:/work
    working_dir: /work/sonic

    # Raw doubles on stdout (see sonic/src/sonic/runtime.ss), so nothing here
    # may allocate a tty and mangle them.
    tty: false

  # PERF DOES NOT WORK ON THIS HOST, AND NO CONTAINER FLAG FIXES IT.
  # kernel.perf_event_paranoid is 4 here, which denies perf_event_open to every
  # unprivileged process -- not just hardware events; `task-clock` fails
  # identically. Measured, all EACCES, because the error is a bare EPERM that
  # names none of this:
  #
  #     seccomp unconfined                  rootless   EACCES
  #     + --cap-add=CAP_PERFMON             rootless   EACCES
  #     + --privileged                      rootless   EACCES
  #     --sysctl kernel.perf_event_paranoid=2          REFUSED (not namespaced)
  #
  # `perfmon_capable()` tests CAP_PERFMON against the INITIAL user namespace and
  # a rootless container's capabilities live in its own, so --privileged is not
  # a stronger --cap-add here; it is the same nothing. Do not spend another
  # afternoon on this flag surface.
  #
  # INSTRUCTION COUNTS DO NOT NEED PERF: harness/count-instructions.sh uses
  # callgrind, which counts by simulation in userspace, needs no capability, and
  # is DETERMINISTIC -- the same binary gives the same number exactly, which is
  # a better answer than perf gave even when perf worked (D57: 11.5% spread).
  # That path only became possible once the runtime stopped emitting AVX-512
  # unconditionally; see D59.
  #
  # The seccomp exception below is kept because it is still NECESSARY (it is the
  # first of the two gates above), just no longer sufficient. What remains
  # genuinely blocked is `perf record` SAMPLING in harness/profile-sonic.sh --
  # "which function holds the cycles" -- for which callgrind is a plausible
  # replacement nobody has written yet.
  #
  # It stays a separate service regardless: the suite keeps the tight default
  # seccomp profile, because the suite runs constantly and is where a compiler
  # bug shows up first, while a benchmark run is short and human-started.
  bench:
    extends:
      service: sonic
    security_opt:
      - seccomp:unconfined
