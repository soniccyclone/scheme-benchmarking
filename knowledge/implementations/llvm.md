---
type: implementation
title: llvm
description: Stub. Referenced by works documents as the reference architecture we deliberately did not adopt. See /decisions/native-back-end.md for why. Not a Lisp implementation, so it carries no implements/lacks rows.
status: draft
tags: [stub]
---
# Stub

Referenced by works documents as the reference architecture we deliberately did not adopt. See /decisions/native-back-end.md for why. Not a Lisp implementation, so it carries no implements/lacks rows.
