---
type: alias
title: explicit control evaluator
description: Merged into continuation-passing-style during wave 2 consolidation. This stub exists so cross-links resolve.
redirects_to: /techniques/continuation-passing-style.md
tags: [alias]
---
# Merged

This facet is covered inside [continuation-passing-style](/techniques/continuation-passing-style.md). It was referenced as a separate
technique by one or more `works/` documents, but is not independent enough to carry its own
document, so wave 2 folded it in.
